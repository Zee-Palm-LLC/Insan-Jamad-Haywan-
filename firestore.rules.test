rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ WARNING: TEST MODE ONLY - ALLOWS ALL ACCESS
    // Enable anonymous auth instead of using these rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

